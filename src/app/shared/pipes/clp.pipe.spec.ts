/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ClpPipe } from './clp.pipe';

describe('Pipe: Clpe', () => {
  it('create an instance', () => {
    let pipe = new ClpPipe();
    expect(pipe).toBeTruthy();
  });
});
